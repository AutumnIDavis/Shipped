<% content_for :title, "Home" %>
 <% if user_signed_in? %>

<div class="row">
	<div class="col-md-12 text-center">
		<% if current_user.user_type == "Job" %>
		<h1 class="ask">You ask .We Ship</h1>
		<% else %>
		<h1 class="ask">You ask .We ship</h1>
		<% end %>
	</div>
</div>

<% else %>
<div class="row">
	<div class="col-md-12 text-center">
		<h1 class="ask">You ask.We ship. </h1>
	</div>
</div>
<div class="signup" id="notauser">
  	<h4><span>...Not a current user? <%= link_to 'Sign Up Here', '/users/sign_up' %></span></h4>
</div>

<% end %>
